'use strict';
// TODO (Phase 3): Load environment variables via dotenv.
// TODO (Phase 3): Export a config object (port, mongoUri, savingsEngine constants).
// Stub – no logic yet.

module.exports = {};
