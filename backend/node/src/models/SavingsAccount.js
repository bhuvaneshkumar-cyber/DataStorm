'use strict';
/**
 * SavingsAccount schema (Mongoose stub)
 *
 * Will store per-user state:
 *   pendingRoundups  : Number  – accumulated round-up cents
 *   pendingSurplus   : Number  – accumulated income-smoothing surplus
 *   incomeHistory    : [Number] – rolling 30-payout window
 *   mandateLimit     : Number  – UPI AutoPay cap (default 1000)
 *   threshold        : Number  – minimum sweep amount (default 100)
 *
 * TODO (Phase 3): Define full schema, virtuals, methods.
 */

// Stub – no logic yet.
module.exports = {};
