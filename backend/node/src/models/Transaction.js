'use strict';
/**
 * Transaction schema (Mongoose stub)
 *
 * Mirrors the Transaction dataclass from savings.py:
 *   amount : float  – transaction value in INR
 *   kind   : str    – "debit" | "platform_payout"
 *
 * TODO (Phase 3): Add userId ref, timestamps, indexes.
 */

// Stub – no logic yet.
module.exports = {};
