# Optional document parsers for POST /analyze-statement.
#
# Kept out of requirements.txt on purpose: they are large, and one of them
# (pytesseract) also needs the tesseract-ocr system binary. The service runs
# fine without them - document_ingestion.py guards every import and the upload
# route answers 503 with a clear message instead of failing at startup.
#
# Install alongside the core set:
#   pip install -r ml_service/requirements.txt -r ml_service/requirements-ingestion.txt
#
# OCR additionally needs the system binary:
#   Debian/Ubuntu:  apt-get install tesseract-ocr
#   Windows:        https://github.com/UB-Mannheim/tesseract/wiki

pdfplumber==0.11.4
pymupdf==1.25.3
python-docx==1.1.2
openpyxl==3.1.5
pytesseract==0.3.10
Pillow==11.1.0
